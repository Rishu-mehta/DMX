# New Feature Command

The orchestrator agent should handle this entirely.

Feature request: $ARGUMENTS

Orchestrator: take this request, break it down,
delegate to the correct specialist agents in the
right order (parallel where possible), run tests
and review, then report final summary.
